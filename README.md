# kahoot-solver
Very useful Python code to retrieve the answers to a Kahoot quiz. Works with ChatGPT, so an API key is required.

Run the code before the Kahoot, then press CTRL+ALT+A to analyze the question. The code will take a screenshot, and deduce the answer. Only works if the question is on the same screen as the answers.

